@model CreateContactViewModel
@{
    ViewData["Title"] = "New Contact";
}

<div class="page-header">
    <div class="page-title">📇 New Contact</div>
    <a asp-controller="Contacts" asp-action="Index" class="btn btn-secondary">← Contacts</a>
</div>

<div class="card" style="max-width:480px;">
    <form asp-controller="Contacts" asp-action="Create" method="post">
        @Html.AntiForgeryToken()
        <div class="form-group">
            <label class="form-label">Name <span style="color:var(--red)">*</span></label>
            <input asp-for="Name" class="form-control" placeholder="Full name…" autofocus />
            <span asp-validation-for="Name" style="color:var(--red);font-size:0.8rem;"></span>
        </div>
        <p style="color:var(--subtext);font-size:0.85rem;margin-bottom:1rem;">
            You can add email addresses, phone numbers, and addresses after saving.
        </p>
        <div style="display:flex;gap:0.5rem;">
            <button type="submit" class="btn btn-primary">Create Contact</button>
            <a asp-controller="Contacts" asp-action="Index" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>

@section Scripts { @await Html.PartialAsync("_ValidationScriptsPartial") }
