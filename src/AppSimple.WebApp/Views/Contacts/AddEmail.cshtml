@model ContactEmailViewModel
@using AppSimple.Core.Enums
@{
    ViewData["Title"] = "Add Email Address";
}

<div class="page-header">
    <div class="page-title">📧 Add Email Address</div>
    <a asp-controller="Contacts" asp-action="Detail" asp-route-uid="@Model.ContactUid" class="btn btn-secondary">← Back</a>
</div>

<div class="card" style="max-width:480px;">
    <form asp-controller="Contacts" asp-action="AddEmail" asp-route-contactUid="@Model.ContactUid" method="post">
        @Html.AntiForgeryToken()
        <div class="form-group">
            <label class="form-label">Email <span style="color:var(--red)">*</span></label>
            <input asp-for="Email" class="form-control" type="email" autofocus />
            <span asp-validation-for="Email" style="color:var(--red);font-size:0.8rem;"></span>
        </div>
        <div class="form-group">
            <label class="form-label">Type</label>
            <select asp-for="Type" asp-items="Html.GetEnumSelectList<EmailType>()" class="form-control"></select>
        </div>
        <div class="form-group" style="display:flex;align-items:center;gap:0.5rem;">
            <input asp-for="IsPrimary" type="checkbox" />
            <label asp-for="IsPrimary" class="form-label" style="margin:0;">Set as primary</label>
        </div>
        <div style="display:flex;gap:0.5rem;margin-top:1rem;">
            <button type="submit" class="btn btn-primary">Add Email</button>
            <a asp-controller="Contacts" asp-action="Detail" asp-route-uid="@Model.ContactUid" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>

@section Scripts { @await Html.PartialAsync("_ValidationScriptsPartial") }
